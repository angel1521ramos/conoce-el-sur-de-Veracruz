---
interface Props {
    h?: string;
    mb?: string;
}
const { h:H = '', mb:Mb = ''} = Astro.props;
---

<div class:list={['flex items-center justify-center rounded bg-gray-50 dark:bg-gray-800', H, Mb]} ><slot /></div>